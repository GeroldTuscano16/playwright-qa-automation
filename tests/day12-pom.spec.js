// tests/day12-pom.spec.js
//const { test, expect } = require('@playwright/test');
//const { GooglePage } = require('../pages/google.page');

//test('Day 12 - Google search using Page Object Model', async ({ page }) => {
 // const googlePage = new GooglePage(page);

  // Open Google
 // await googlePage.open();

  // Search for a term
  //await googlePage.search('Playwright Page Object Model');

  // Simple check to make sure results page has results
  //await expect(page).toHaveTitle(/Playwright/);
//});
